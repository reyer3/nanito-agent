name: build-dashboard
description: Build a BI dashboard from data — schema, queries, visualizations

inputs:
  - data_source: string
  - metrics: string

steps:
  - agent: predictor
    task: "Premortem for building a dashboard on {{data_source}} with metrics: {{metrics}}. Data quality risks? Performance bottlenecks?"
    output: premortem.md

  - agent: architect
    task: "Design dashboard architecture for {{data_source}} with metrics: {{metrics}}. Consider premortem findings."
    output: spec.md

  - parallel:
    - agent: api-designer
      task: "Design the data API — queries, aggregations, filters"
      output: api-spec.yaml
    - agent: frontend-builder
      task: "Design the dashboard layout and visualization components"
      output: ui-spec.md

  - agent: devops-guard
    task: "Safety check before implementation"

  - parallel:
    - agent: implementer
      task: "Build the data API and query layer"
      worktree: true
    - agent: implementer
      task: "Build the dashboard frontend with charts and filters"
      worktree: true

  - agent: devops-guard
    task: "Post-implementation safety check"

  - agent: tester
    task: "Write tests for data queries and dashboard interactions"

  - agent: reviewer
    task: "Final review — data correctness, performance, UX. Cross-reference premortem."
